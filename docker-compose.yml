services:
    sounder-backend:
        container_name: sounder-backend
        restart: always
        build: .
        ports:
            - 8080:8080
        volumes:
            - ./pb:/pb/pb_data
            - ./pb_hooks:/pb/pb_hooks
        networks:
            - network-sounder

networks:
    network-sounder:
        name: network-sounder
